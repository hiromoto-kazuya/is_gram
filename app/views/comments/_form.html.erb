<%= form_with(model: [post, comment]) do |form| %>

  <div class = "form-group">
    <%= form.text_field :content, class: "form-control",placeholder: "コメントを追加"  %>
  </div>

  <div class="actions">
    <%= form.submit 'コメントする', class: "comments-btn" %>
  </div>
<% end %>
